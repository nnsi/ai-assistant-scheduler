# Lefthook configuration
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: false
  commands:
    typecheck:
      glob: "packages/**/*.{ts,tsx}"
      run: pnpm typecheck
    test:
      glob: "packages/**/*.{ts,tsx}"
      run: pnpm test --run
